# 衡堕博客设计规范

> 本文档把隐性的视觉偏好转成可复用约束。实际数值以 `src/styles/design-tokens.css` 为唯一来源。

## 1. 品牌与气质

- 定位：学习笔记与项目记录，面向技术阅读。
- 气质：克制、专业、冷静；极客工作室 + 轻量数据感。
- 主色：冷青 teal；暗色背景使用深靛蓝分层。
- 禁止：高饱和霓虹堆叠、玻璃拟态泛滥、3D 装饰、自动生成文章封面、无意义大面积动效。

## 2. 色彩

- 品牌强调色：沿用运行时 `--primary`；固定光晕参考色 `#72D8D2`。
- 暗色页面：`#1F2540`；卡片：`#2B3452`；浮层：`#30395B`。
- 主文字：暗色 `#F5F7FF`；辅助文字通过现有 `text-75 / text-50 / text-30` 层级表达。
- 强调色面积不超过单屏约 10%，用于链接、焦点、进度和交互反馈。

## 3. 排版

- 中文正文/UI：Noto Sans SC Variable。
- 代码、统计和系统标签：JetBrains Mono Variable。
- 正文：`--ds-text-body`，行高 `--ds-leading-body`，单行阅读宽度不超过约 72 个字符。
- 标题：使用 `--ds-text-display / title / heading-2 / heading-3`，中文标题采用轻微负字距并允许平衡换行。
- 元数据：`--ds-text-meta`，使用等宽数字。

## 4. 间距与组件

- 使用 4px 基线：`--ds-space-1` 至 `--ds-space-10`。
- 卡片圆角：`--ds-radius-card`；控件圆角：`--ds-radius-control`。
- 卡片默认只有细边与低阴影；hover 最多上移 2px，不缩放整个卡片。
- 标签使用轻微上浮和底部沿线高亮，不使用弹跳。

## 5. 动效

- 快速反馈：`--ds-motion-fast`（180ms）。
- 常规组件：`--ds-motion-base`（320ms）。
- 页面切换：`--ds-motion-page`（420ms）。
- 缓动：`cubic-bezier(0.16, 1, 0.3, 1)`。
- 页面位移不超过 8px，模糊不超过 2px。
- 必须完整支持 `prefers-reduced-motion: reduce`。

## 6. 内容与可访问性

- 文章卡片保持无封面文字流；除非先完成审美确认，不添加装饰性封面。
- 正文链接、标签、按钮必须有可见 hover/focus 状态。
- 复制按钮必须反馈成功或失败，并同步更新 `aria-label`。
- 页面切换、复制反馈不能阻断阅读或键盘操作。
